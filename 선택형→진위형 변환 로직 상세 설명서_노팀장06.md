# 선택형→진위형 변환 로직 상세 설명서

**작성일**: 2025년 7월 22일  
**작성자**: 노팀장  
**수신**: 코코치(나실장)  
**목적**: 변환 로직의 구체적 구현 가이드 제공

---

## 1. 변환 로직 핵심 원리

### 1.1 기본 공식
```
1개 선택형 문제 (4개 선택지) 
= 8개 진위형 문제 (각 선택지 × 긍정/부정)

선택지별 변환:
- 선택지 1 → TF01(긍정), TF02(부정)
- 선택지 2 → TF03(긍정), TF04(부정)  
- 선택지 3 → TF05(긍정), TF06(부정)
- 선택지 4 → TF07(긍정), TF08(부정)
```

### 1.2 정답 결정 로직

#### **"옳은 것 고르기" 유형**
```javascript
원본: 정답이 3번 선택지인 경우

TF01: 1번선택지는 [주제]이다 → X (틀린 선택지이므로)
TF02: 1번선택지는 [주제]가 아니다 → O (틀린 선택지이므로)
TF03: 2번선택지는 [주제]이다 → X (틀린 선택지이므로)
TF04: 2번선택지는 [주제]가 아니다 → O (틀린 선택지이므로)
TF05: 3번선택지는 [주제]이다 → O (정답 선택지이므로)
TF06: 3번선택지는 [주제]가 아니다 → X (정답 선택지이므로)
TF07: 4번선택지는 [주제]이다 → X (틀린 선택지이므로)
TF08: 4번선택지는 [주제]가 아니다 → O (틀린 선택지이므로)
```

#### **"틀린 것 고르기" 유형**
```javascript
원본: 정답이 3번 선택지인 경우

TF01: 1번선택지는 [주제]이다 → O (옳은 선택지이므로)
TF02: 1번선택지는 [주제]가 아니다 → X (옳은 선택지이므로)
TF03: 2번선택지는 [주제]이다 → O (옳은 선택지이므로)
TF04: 2번선택지는 [주제]가 아니다 → X (옳은 선택지이므로)
TF05: 3번선택지는 [주제]이다 → X (틀린 선택지이므로)
TF06: 3번선택지는 [주제]가 아니다 → O (틀린 선택지이므로)
TF07: 4번선택지는 [주제]이다 → O (옳은 선택지이므로)
TF08: 4번선택지는 [주제]가 아니다 → X (옳은 선택지이므로)
```

---

## 2. 구체적 변환 예시

### 2.1 보험 전문 문제 예시

#### **원본 선택형 문제**
```
QCODE: ABANK-0005
문제: 화재보험의 종류에 대한 다음 설명 중 옳은 것은?
1) 일반화재보험은 상업용 건물만 보장한다
2) F.O.C Policy는 1905년에 만들어졌다  
3) 일반화재보험은 일반물건과 공장물건을 보장한다
4) Fire Insurance Policy는 대규모 기업 전용이다
정답: 3번
```

#### **변환된 진위형 문제**
```
ABANK-0005-TF01:
문제: 일반화재보험은 상업용 건물만 보장한다
정답: X (틀린 선택지를 긍정했으므로)

ABANK-0005-TF02:
문제: 일반화재보험이 상업용 건물만 보장한다는 것은 틀리다
정답: O (틀린 선택지를 부정했으므로)

ABANK-0005-TF03:
문제: F.O.C Policy는 1905년에 만들어졌다
정답: X (틀린 선택지를 긍정했으므로)

ABANK-0005-TF04:
문제: F.O.C Policy가 1905년에 만들어졌다는 것은 틀리다
정답: O (틀린 선택지를 부정했으므로)

ABANK-0005-TF05:
문제: 일반화재보험은 일반물건과 공장물건을 보장한다
정답: O (정답 선택지를 긍정했으므로)

ABANK-0005-TF06:
문제: 일반화재보험이 일반물건과 공장물건을 보장한다는 것은 틀리다
정답: X (정답 선택지를 부정했으므로)

ABANK-0005-TF07:
문제: Fire Insurance Policy는 대규모 기업 전용이다
정답: X (틀린 선택지를 긍정했으므로)

ABANK-0005-TF08:
문제: Fire Insurance Policy가 대규모 기업 전용이라는 것은 틀리다
정답: O (틀린 선택지를 부정했으므로)
```

---

## 3. 프로그래밍 구현 로직

### 3.1 의사코드 (Pseudocode)
```javascript
function convertToTrueFalse(originalQuestion) {
    // 1. 문제 유형 판별
    const questionType = detectQuestionType(originalQuestion.QUESTION);
    // "옳은" 포함 → CORRECT_TYPE
    // "틀린" 포함 → WRONG_TYPE
    
    // 2. 선택지 추출
    const choices = extractChoices(originalQuestion.QUESTION);
    // 정규식으로 1), 2), 3), 4) 또는 ①, ②, ③, ④ 추출
    
    // 3. 정답 인덱스
    const correctIndex = parseInt(originalQuestion.ANSWER) - 1;
    
    // 4. 8개 진위형 생성
    const trueFalseQuestions = [];
    
    for (let i = 0; i < choices.length; i++) {
        // 긍정형 (홀수 번호)
        const positiveQuestion = {
            QCODE: `${originalQuestion.QCODE}-TF${String(i*2+1).padStart(2, '0')}`,
            QUESTION: choices[i], // 원본 선택지 그대로
            ANSWER: determineAnswer(questionType, i, correctIndex, true)
        };
        
        // 부정형 (짝수 번호)  
        const negativeQuestion = {
            QCODE: `${originalQuestion.QCODE}-TF${String(i*2+2).padStart(2, '0')}`,
            QUESTION: makeNegativeStatement(choices[i]),
            ANSWER: determineAnswer(questionType, i, correctIndex, false)
        };
        
        trueFalseQuestions.push(positiveQuestion, negativeQuestion);
    }
    
    return trueFalseQuestions;
}

function determineAnswer(questionType, currentIndex, correctIndex, isPositive) {
    const isCorrectChoice = (currentIndex === correctIndex);
    
    if (questionType === "CORRECT_TYPE") {
        // "옳은 것 고르기"
        if (isPositive) {
            return isCorrectChoice ? "O" : "X";
        } else {
            return isCorrectChoice ? "X" : "O";
        }
    } else if (questionType === "WRONG_TYPE") {
        // "틀린 것 고르기"  
        if (isPositive) {
            return isCorrectChoice ? "X" : "O";
        } else {
            return isCorrectChoice ? "O" : "X";
        }
    }
}
```

### 3.2 선택지 추출 정규식
```javascript
function extractChoices(questionText) {
    // 패턴 1: 1), 2), 3), 4)
    const pattern1 = /(\d+\))\s*([^①②③④⑤\d\)]+?)(?=\d+\)|$)/g;
    
    // 패턴 2: ①, ②, ③, ④  
    const pattern2 = /([①②③④⑤])\s*([^①②③④⑤\d\)]+?)(?=[①②③④⑤]|$)/g;
    
    // 추출 및 정제
    const matches = [...questionText.matchAll(pattern1)] || 
                   [...questionText.matchAll(pattern2)];
    
    return matches.map(match => match[2].trim())
                 .filter(choice => choice.length > 3);
}
```

---

## 4. 데이터 구조 설계

### 4.1 변환된 문제 CSV 구조
```csv
INDEX,QCODE,SOURCE,LAYER1,LAYER2,LAYER3,TYPE,CODE1,CODE2,TITLE,QUESTION,INPUT,RESULT,ANSWER,풀이회수,정답회수,오답회수,오답율,EXPLAIN,ANALSYS,1ST,2ND,3rd,4th,5th,6th,7th,8th,9th,10th
1380,ABANK-0005-TF01,인스교재,06재산보험,화재보험,개요,진위형,AA01,AA01,화재보험의 종류,일반화재보험은 상업용 건물만 보장한다,,X,X,0,0,0,,원본문제 ABANK-0005에서 변환,,,,,,,,,,
1381,ABANK-0005-TF02,인스교재,06재산보험,화재보험,개요,진위형,AA01,AA01,화재보험의 종류,일반화재보험이 상업용 건물만 보장한다는 것은 틀리다,,O,O,0,0,0,,원본문제 ABANK-0005에서 변환,,,,,,,,,,
```

### 4.2 변환 추적 메타데이터
```javascript
const conversionMetadata = {
    "ABANK-0005-TF01": {
        originalQCODE: "ABANK-0005",
        choiceIndex: 0,
        statementType: "POSITIVE",
        originalChoice: "일반화재보험은 상업용 건물만 보장한다",
        conversionDate: "2025-07-22"
    },
    "ABANK-0005-TF02": {
        originalQCODE: "ABANK-0005", 
        choiceIndex: 0,
        statementType: "NEGATIVE",
        originalChoice: "일반화재보험은 상업용 건물만 보장한다",
        conversionDate: "2025-07-22"
    }
    // ... 나머지 TF03~TF08
};
```

---

## 5. 품질 보증 체크리스트

### 5.1 변환 후 검증 항목
- [ ] 원본 지문 내용 100% 보존
- [ ] 새로운 전문용어 추가 없음
- [ ] 정답 로직 정확성 검증
- [ ] QCODE 중복 없음
- [ ] 모든 메타데이터 완성

### 5.2 샘플 테스트 진행
1. **5개 문제 샘플 변환**
2. **조대표님 검토 및 승인**
3. **로직 수정 및 개선**
4. **전체 68개 문제 변환**

---

## 6. 구현 가이드라인

### 6.1 절대 원칙
- **원본 지문 변경 금지**: 선택지 텍스트 그대로 사용
- **전문용어 보존**: 보험 전문용어 정확히 유지
- **로직 일관성**: 모든 문제에 동일한 변환 규칙 적용

### 6.2 예외 처리
- **선택지 추출 실패**: 수동 검토 후 처리
- **애매한 문제 유형**: "옳은 것" 기본값 적용
- **특수 문자 처리**: CSV 호환성 고려

---

**이 상세 로직으로 코코치가 완벽한 변환 시스템을 구현할 수 있을 것입니다!** 🚀