# 데이터로딩형 퀴즈 앱에서 서대리 실패원인 분석.md

## 1. 개요
- 본 문서는 AICU 퀴즈앱의 데이터 분리형(외부 CSV 로딩) 구현에서 노팀장(no_20.html+sample_20.csv)과 서대리(index.html) 코드의 구조, 데이터 처리 방식, 성공/실패 원인을 비교 분석합니다.

---

## 2. 노팀장(no_20.html + sample_20.csv) 방식 분석

### 2.1 구조 및 특징
- **문제 데이터**: 외부 CSV(`sample_20.csv`)를 fetch+PapaParse로 동적으로 로딩
- **분리형 설계**: 문제 데이터와 앱 로직을 완전히 분리(데이터 교체/확장 용이)
- **파싱 로직**: PapaParse로 헤더 기반 파싱, `questions = parsed.data.filter(row => row.QCODE && row.QUESTION);`로 유효 데이터만 추출
- **UI/UX**: Tailwind 기반, 진행률/통계/문제/정답/컨트롤 등 필수 기능만 구현
- **실행 흐름**: window.onload → loadCSVData() → 파싱 성공 시 startQuiz()로 앱 시작
- **에러 처리**: fetch/파싱 실패 시 명확한 에러 메시지 및 해결 방법 안내
- **환경 적응성**: GitHub Pages 등 웹 환경에서 fetch로 CSV 정상 로딩 보장

### 2.2 성공 요인
- **파싱 후 데이터 유효성 검증**: QCODE, QUESTION 등 필수 필드 존재 여부로 유효 데이터만 questions 배열에 저장
- **필드명/인코딩 이슈 최소화**: CSV 헤더와 JS 필드명 일치, BOM/공백/대소문자 문제 없음
- **실행 신뢰성**: 데이터 로딩 실패 시 앱이 멈추지 않고, 명확한 안내 제공
- **단순화된 로직**: 불필요한 상태/필터링/모드 분기 최소화로 오류 가능성 최소화

---

## 3. 서대리(index.html) 방식 분석

### 3.1 구조 및 특징
- **문제 데이터**: 외부 CSV(`ins_789_final.csv`)를 fetch+PapaParse로 파싱 후 questions 배열에 저장
- **상태/모드 분기**: 다양한 학습 모드, 대분류, 오답노트 등 고급 기능 구현(확장성 높음)
- **파싱 로직**: PapaParse로 파싱 후 questions 배열에 저장, 이후 `instMaterialQuestions = questions.filter(q => q.SOURCE === '인스교재');`로 필터링
- **실행 흐름**: window.onload → loadCSVData() → 파싱 후 다양한 상태/모드 분기
- **에러 처리**: fetch/파싱 실패 시 UI에 에러 메시지 표시(실행 중단)

### 3.2 실패 원인
- **필드명 불일치/인코딩 이슈**: CSV 헤더에 BOM, 공백, 대소문자 차이 등으로 인해 `q.SOURCE === '인스교재'` 필터가 항상 0개 반환 → 문제 데이터 미표시
- **파싱 후 유효성 검증 미흡**: 필수 필드(QCODE, QUESTION 등) 존재 여부 확인 없이 전체 데이터를 questions 배열에 저장
- **상태/분기 복잡성**: 다양한 모드/분기/필터링 로직이 얽혀 있어, 데이터가 비어있을 때 원인 진단이 어려움
- **에러 fallback 부족**: 데이터 로딩 실패 시 샘플 데이터/대체 안내 등 fallback 미제공

---

## 4. 비교 및 결론

| 항목                | 노팀장(no_20.html)         | 서대리(index.html)           |
|---------------------|-----------------------------|------------------------------|
| 데이터 로딩         | CSV fetch+파싱+유효성검증   | CSV fetch+파싱(유효성 미흡)  |
| 필드명/인코딩 이슈  | 없음(헤더-필드명 일치)      | 있음(BOM, 공백, 대소문자 등) |
| 파싱 후 필터링      | QCODE/QUESTION 존재만 체크  | SOURCE === '인스교재' 필터    |
| 실행 신뢰성         | 높음(에러 안내/복구 용이)   | 낮음(데이터 미표시 반복)      |
| 확장성/유지보수     | 높음(데이터 교체 용이)      | 높음(모드/상태 다양)         |

### 결론
- **노팀장 방식**은 데이터 유효성 검증, 필드명 일치, 단순화된 로직으로 환경/인코딩 이슈 없이 신뢰성 높은 실행을 보장
- **서대리 방식**은 확장성은 높으나, 필드명/인코딩/파싱 후 필터링 오류로 데이터가 정상적으로 로드되지 않아 앱이 동작하지 않음
- **핵심 실패 원인**: 파싱 후 필드명 불일치 및 유효성 검증 미흡으로, questions 배열이 비거나 필터링 결과가 0개가 되어 문제 데이터가 표시되지 않음

---

## 5. 개선 제안
- CSV 파싱 후 필드명 정규화, BOM/공백/대소문자 처리, 필수 필드(QCODE, QUESTION 등) 존재 여부로 유효성 검증
- 필터 조건 강화: `q.SOURCE && q.SOURCE.trim() === '인스교재'` 등
- 데이터 로딩 실패 시 fallback(샘플 데이터, 에러 메시지 등) 제공
- 복잡한 상태/분기 로직은 데이터 유효성 검증 후에만 실행 