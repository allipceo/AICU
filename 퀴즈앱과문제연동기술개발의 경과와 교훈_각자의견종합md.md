#### 노팀장의 분석
#### 서대리의 분석

Ran tool
Ran tool
Ran tool
Read file: no_20.html
Ran tool
Read file: sample_20.csv
Read file: index.html
아래는 요청하신 분석 보고서의 요약입니다.  
(전체 보고서는 `데이터로딩형 퀴즈 앱에서 서대리 실패원인 분석.md` 파일로 저장되었습니다.)

---

# 데이터로딩형 퀴즈 앱에서 서대리 실패원인 분석.md

## 1. 개요
- 노팀장(no_20.html+sample_20.csv)과 서대리(index.html) 코드의 데이터 로딩/파싱 구조, 성공/실패 원인, 차이점, 개선방안을 비교 분석합니다.

---

## 2. 노팀장(no_20.html + sample_20.csv) 방식 분석

- **문제 데이터**: 외부 CSV(sample_20.csv)를 fetch+PapaParse로 동적으로 로딩
- **분리형 설계**: 문제 데이터와 앱 로직을 완전히 분리(데이터 교체/확장 용이)
- **파싱 로직**: PapaParse로 헤더 기반 파싱, QCODE/QUESTION 필수 필드 존재 여부로 유효 데이터만 추출
- **실행 신뢰성**: 데이터 로딩 실패 시 명확한 안내 제공, 단순화된 로직으로 오류 가능성 최소화

**성공 요인**
- 파싱 후 데이터 유효성 검증(QCODE, QUESTION 등)
- 필드명/인코딩 이슈 최소화(헤더-필드명 일치, BOM/공백/대소문자 문제 없음)
- 단순화된 로직, 환경 적응성(웹 어디서나 fetch로 CSV 정상 로딩)

---

## 3. 서대리(index.html) 방식 분석

- **문제 데이터**: 외부 CSV(ins_789_final.csv)를 fetch+PapaParse로 파싱 후 questions 배열에 저장
- **상태/모드 분기**: 다양한 학습 모드, 대분류, 오답노트 등 고급 기능 구현(확장성 높음)
- **파싱 로직**: PapaParse로 파싱 후 questions 배열에 저장, 이후 SOURCE === '인스교재' 필터링
- **실행 신뢰성**: 데이터 미표시 반복, 에러 fallback 부족

**실패 원인**
- 필드명 불일치/인코딩 이슈(BOM, 공백, 대소문자 차이 등)로 필터가 항상 0개 반환
- 파싱 후 유효성 검증 미흡(필수 필드 존재 여부 확인 없이 전체 데이터 저장)
- 복잡한 상태/분기 로직, 데이터가 비어있을 때 원인 진단 어려움

---

## 4. 비교 및 결론

| 항목                | 노팀장(no_20.html)         | 서대리(index.html)           |
|---------------------|-----------------------------|------------------------------|
| 데이터 로딩         | CSV fetch+파싱+유효성검증   | CSV fetch+파싱(유효성 미흡)  |
| 필드명/인코딩 이슈  | 없음(헤더-필드명 일치)      | 있음(BOM, 공백, 대소문자 등) |
| 파싱 후 필터링      | QCODE/QUESTION 존재만 체크  | SOURCE === '인스교재' 필터    |
| 실행 신뢰성         | 높음(에러 안내/복구 용이)   | 낮음(데이터 미표시 반복)      |
| 확장성/유지보수     | 높음(데이터 교체 용이)      | 높음(모드/상태 다양)         |

**핵심 실패 원인**  
- 파싱 후 필드명 불일치 및 유효성 검증 미흡으로, questions 배열이 비거나 필터링 결과가 0개가 되어 문제 데이터가 표시되지 않음

---

## 5. 개선 제안

- CSV 파싱 후 필드명 정규화, BOM/공백/대소문자 처리, 필수 필드(QCODE, QUESTION 등) 존재 여부로 유효성 검증
- 필터 조건 강화: `q.SOURCE && q.SOURCE.trim() === '인스교재'` 등
- 데이터 로딩 실패 시 fallback(샘플 데이터, 에러 메시지 등) 제공
- 복잡한 상태/분기 로직은 데이터 유효성 검증 후에만 실행

---

**이 보고서는 실전 데이터로딩형 퀴즈앱 개발 시  
실패를 방지하기 위한 핵심 교훈을 담고 있습니다.  
(전체 파일은 저장 완료, 추가 요청시 언제든 보완 가능합니다.)**

#### 코코치의 분석